#name: CI
#on: [push, pull_request]
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions/setup-node@v4
#        with:
#          node-version: '20'
#      - run: npm ci
#      - run: npm test

name: CI
on:     
  push:
    branches: [ main, develop, feature/** ]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true  # cancel old test if new update, saving time.

jobs:
  quality-and-tests:
    # Ubuntu Runner，easy for Node.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install
        run: npm ci

      # Lint：Static Rule checking: 
      - name: Lint
        run: npm run lint --if-present

      # Type Check：TypeScript: Type checking
      - name: Type check
        run: npm run typecheck --if-present

      - name: Unit tests (with coverage)
        run: npm test -- --coverage

      - name: Build (ensure it actually compiles)
        run: npm run build --if-present

      - name: Upload coverage (artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: coverage/lcov.info